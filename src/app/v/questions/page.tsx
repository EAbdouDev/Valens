import { FC } from "react";
import { auth } from "../../../../firebase/server";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return <div>userId: {}</div>;
};

export default page;
