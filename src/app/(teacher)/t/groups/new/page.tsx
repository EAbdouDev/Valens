import NewGroup from "@/components/teacher/groups/NewGroup";
import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <div>
      <NewGroup />
    </div>
  );
};

export default page;
